package dev.litemfinder.navigation;

/** Modes are extensible at graph-provider boundaries; MODDED edges carry provider-specific conditions. */
public enum TravelMode {
    WALK,
    ICE_BOAT,
    WATER_BOAT,
    RAIL,
    PORTAL,
    ELYTRA,
    MODDED
}
